# Cruzer — dependencias del ejecutable.
# El servidor usa sólo stdlib (http.server) a propósito: menos deps = .exe más
# chico y build de PyInstaller menos frágil. Estas son las de los engines.
playwright>=1.40
playwright-stealth>=1.0.6
pandas>=2.0
openpyxl>=3.1
beautifulsoup4>=4.12
Pillow>=10.0
# Ventana nativa (app propia con ícono en la barra de tareas). Usa el motor web
# del SO: WebView2/Edge en Windows (pythonnet), WKWebView en Mac (pyobjc).
# Si falta o falla en runtime, el shell cae al navegador (fallback).
pywebview>=5.0
pythonnet>=3.0 ; sys_platform == "win32"
certifi>=2023.7.22
